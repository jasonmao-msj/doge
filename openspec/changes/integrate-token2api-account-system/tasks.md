# Tasks: token2api Account Convenience v1

> Execution status: `bounded-local-trial-implementation / unsigned-package-built`。当前 implementation fact 与验证证据见 `verification.md`；下列 checkbox 仍按原 A0/GA gate 严格记账，未因 bounded trial implementation 自动勾满。早期 lane/dispatch 文案保留为历史执行规划。
>
> Task notation: `[Priority][Owner][Depends][Gate][Output][Validation]`。所有 path 均为 repo-relative；token2api work 明确写作 `token2api:<path>`。checkbox 只有在对应 output 与 validation evidence 均存在时才可勾选。

## 0. Governance、ownership 与 artifact truth

- [x] 0.1 [P0][Owner: documentation-governance-owner][Depends: none][Gate: G0][Output: frozen `proposal.md`、`design.md`、behavior spec、PRD 与 readiness research inputs][Validation: `design.md` 状态为 `architecture-frozen-for-contract-implementation`；M0/A0 truth、B-01–B-12 resolution、T-GAP-01–13 与 target macOS trial scope 均可追溯]
- [x] 0.2 [P0][Owner: documentation-governance-owner][Depends: 0.1][Gate: dispatch planning][Output: 本 `tasks.md` 的 dependency、owner、exit gate 与 M0/A0 wording][Validation: `openspec validate integrate-token2api-account-system --type change --strict --no-interactive` 与 `git diff --check`]
- [ ] 0.3 [P0][Owner: doge-project-lead][Depends: 0.2][Gate: before writes — first batch missed; `research/dispatch-integration-ledger.md#2-governance-deviation-gd-001`][Output: future-wave pre-write Dispatch Plan + reconstructed first-batch ownership record；首批没有 durable consolidated pre-write artifact，post-hoc ledger不得冒充历史合规][Validation: 保持未勾，直到lead明确处置GD-001并批准remediation acceptance；后续write wave必须在写入前冻结exclusive ownership/non-goals/reviewers/shared zones，所有brief含collaboration notice且actual diff无重叠]
- [ ] 0.4 [P0][Owner: doge-project-lead + documentation-governance-owner][Depends: 0.2; tracks unresolved 0.3 deviation][Gate: continuous][Output: `research/dispatch-integration-ledger.md`，持续记录每个handoff、actual diff snapshot、contract version、resolved finding、unverified item与owner][Validation: active change期间保持未勾；Mock/backend/conformance/Real/M0/package/A0 verdict分别记录，不以任一lane green冒充端到端完成；closure时所有未验证项已有owner或explicit deferred change]

### Serialized conflict zones

以下 surface 不得分配给首批并行 lane；只能在对应 integration task 中串行修改：

- canonical contract/schema/scenario/forbidden corpus：仅 C0 contract owner；各 lane只写 derived projection。
- `src/features/app/hooks/useSettingsModalState.ts`、`src/features/settings/**`、`src/app-shell-parts/**`、style loaders：仅 F6 frontend integration owner。
- `src/services/tauri.ts`、`src/services/tauri.test.ts`、`src-tauri/src/{command_registry.rs,lib.rs,state.rs}`：仅 D7 IPC integration owner。
- `src-tauri/Cargo.toml`、`src-tauri/Cargo.lock`：仅 D6 dependency integration owner。
- 十个 locale `index.ts`：仅 F6 i18n integration owner。
- Codex target/provider/session runtime shared files：仅 C3 Codex recipe/runtime owner，在 D5 config contract green 后写入。

## 1. G1 executable contract freeze — C0 single-owner foundation

- [x] 1.1 [P0][Owner: quality-engineer as designated C0 contract owner][Depends: 0.3][Gate: C0 foundation][Output: canonical secret-free `doge-account-semantic/1.0.0` schema、closed capability/state/error/outcome enums、three identity rules (`GatewayIntentId` / `TransportRequestId` / `BrokerOperationId`) 与 B-01–B-12 frozen resolutions][Validation: schema Good/Base/Bad；closed union exhaustiveness；unsupported major/unknown enum/missing guarantee均 fail Account Convenience closed且 Local Mode不变]
- [x] 1.2 [P0][Owner: designated C0 contract owner][Depends: 1.1][Gate: C0 foundation][Output: `doge-account-gateway/1.0.0` composed `AccountGatewayV1` port schema，含 `reconcileIntent`、`auth.readOAuthAttempt`、complete auth/profile/security/usage/config operation families与 discriminated recovery model][Validation: 删除 `AccountService`/flat-port/session-only/raw URL/message branches；TS compile-time fixture证明 Mock/Real满足同一 Gateway]
- [x] 1.3 [P0][Owner: designated C0 contract owner][Depends: 1.1][Gate: C0 foundation][Output: `doge-account-ipc/1.0.0`、`doge-account-broker/1.0.0`、`token2api-account-authority/1.0.0`、`doge-config-recipe/1.0.0`、`doge.account.codex-token-service/1` 与 persistence schema v1 executable schemas][Validation: TS/Rust/Authority round-trip fixtures覆盖每个 operation/result/event 的 null/optional/decimal-string/SemVer semantics；raw token/ticket/URL/path/content/diff/persistence entity无法出现在 safe output type]
- [x] 1.4 [P0][Owner: designated C0 contract owner][Depends: 1.1, 1.2, 1.3][Gate: C0 foundation][Output: one canonical `ScenarioManifestV1`，纳入 complete register/login/MFA/OAuth/reset/restore/logout/profile/security、pull-only usage、managed-key、Codex plan/apply/recovery、Local Mode、version/unknown/race scenarios与research alias normalization][Validation: unique IDs、immutable existing semantics、`requiredLanes`、terminal truth、reset/replay、Local Mode invariant与 secret-negative assertions由validator机械校验]
- [x] 1.5 [P0][Owner: designated C0 contract owner][Depends: 1.1–1.4][Gate: C0 foundation][Output: manifest/schema validators、synthetic Good/Base/Bad fixture pack、forbidden field/value/PII/path corpus与 lane projection contract][Validation: malformed/duplicate/secret-bearing fixture必失败；canonical fixtures不含真实email、credential、production URL或raw path/diff；test-automation-engineer仅review/consume，不并行修改canonical files]
- [ ] 1.6 [P0][Owner: type-contract-reviewer + security-privacy-reviewer][Depends: 1.1–1.5][Gate: G1][Output: read-only Type Contract Review 与 Threat Review findings全部关闭，覆盖optional/nullability、timestamp、integer precision、opaque handles、epoch monotonicity、purpose-scoped inputs、vault/callback/config/privacy boundaries；修订只由designated C0 contract owner落盘][Validation: zero unresolved blocking finding；reviewers不修改canonical files；B-01–B-12均有exact executable enforcement site]
- [ ] 1.7 [P0][Owner: documentation-governance-owner][Depends: 1.6; current 0.4 ledger checkpoint][Gate: G1 exit][Output: executable contract freeze evidence与 supported contract version ledger][Validation: canonical schemas、manifest、validators、Good/Base/Bad fixtures、version rules和forbidden corpus均存在并被C0/F/D/T owners接受；Type与Security针对同一final digest通过re-review；ledger已记录blocker closure与remaining unverified；strict OpenSpec通过]

## 2. First parallel dispatch wave — disjoint leaf ownership

> Dispatch rule：F0/D0/T0 可在 C0 contract path/name/owner 冻结后并行搭建 leaf shell；在 1.7 `G1` 前不得合并私有字段、注册 shared commands、启用 Real composition或声称 lane readiness。

- [ ] 2.1 [P0][Owner: frontend-engineer as Lane F][Depends: 1.2 path/shape freeze][Gate: F0][Output: `src/features/account/**`（排除 C0-owned `contracts/**`）中的 `AccountGatewayV1` consumer shell、compile-time DEV/test-only Account Lab、deterministic virtual clock、scenario runtime core、zero-call guards与feature-local tests/CSS/locale leaf files][Validation: unknown scenario closed failure；seed/reset/advance/fault/history deterministic；real `fetch`/Tauri `invoke`/native calls=0；不修改 Settings/AppShell/Tauri barrel/locale indexes]
- [ ] 2.2 [P0][Owner: backend-runtime-engineer as Lane D][Depends: 1.3 path/shape freeze][Gate: D0][Output: `src-tauri/src/account/**`（排除 C0-owned contract files和shared registration）Rust broker skeleton、stateful fake authority、fault-injecting in-memory vault/repository/clock、intent→operation binding与 Local Mode isolation harness][Validation: no real token2api/network、OS vault、shared command registry或Codex runtime writes；cancel-before-send、outcomeUnknown、reconcile、epoch fence与 fake terminal receipt tests]
- [x] 2.3 [P0][Owner: token2api API owner + security-privacy-reviewer as Lane T][Depends: 1.3 authority shape freeze][Gate: T0 planning][Output: token2api clean-baseline/upstream-sync plan，current route→handler/service/schema inventory，M1–M10/A1–A3/T-GAP mapping、migration/rollback/PR split、repo approval ledger与 sanitized Authority fixtures plan][Validation: 明确 current vs target；`02e50cc22` security baseline、origin/main drift、token2api `AGENTS.md` approval/upstream impact均有 evidence；此 task不修改 token2api source/schema/deployment]
- [ ] 2.4 [P0][Owner: quality-engineer][Depends: 2.1, 2.2, 2.3][Gate: first-wave boundary audit][Output: first-wave ownership/conformance audit][Validation: C0/F0/D0/T0 actual diff互不重叠；F0零真实调用；D0仅fake authority；T0仅planning/evidence；shared registries仍未修改]

## 3. Lane F — complete Mock-first Account experience

- [ ] 3.1 [P0][Owner: frontend-engineer][Depends: 1.7, 2.1][Gate: F1][Output: Mock-driven public capability bootstrap、register/direct-or-email-code、login、persistent-only completion、validation/rate-limit/policy/offline/latency journeys][Validation: manifest F1 scenarios；access-only/vault-unavailable绝不投影authenticated；Local Mode始终可进入]
- [ ] 3.2 [P0][Owner: frontend-engineer + product-design-owner][Depends: 3.1][Gate: F2][Output: MFA challenge/retry/expiry、OAuth approve/deny/create-bind/MFA/callback states、forgot/reset link return、cold restore/logout/revoke-unconfirmed Mock journeys][Validation: early/lost/duplicate/late event通过authoritative read收敛；无URL/token/ticket进入Gateway view或generic store]
- [ ] 3.3 [P0][Owner: frontend-engineer][Depends: 3.1][Gate: F3][Output: Settings Account Center feature-local shell、profile/security surfaces与 pull-only quota/usage view，保留source/fetchedAt/freshness/stale/unavailable][Validation: Account root mount与background不fetch usage；仅usage view open或explicit refresh读取一次；stale response受generation fence丢弃；quota不得gate Local Mode]
- [ ] 3.4 [P0][Owner: frontend-engineer + product-design-owner][Depends: 3.3, 1.3][Gate: F4][Output: Codex configuration Mock UI：adaptive offer、changed-file list、lazy semantic redacted detail、exact-plan consent、apply/result/recovery、ordinary close/ack/bubble reopen/HardDismiss][Validation: login和managed-key consent不等于apply consent；applying不可hard dismiss；ack只清当前result badge；raw path/content/diff不存在于frontend types]
- [ ] 3.5 [P1][Owner: accessibility-localization-reviewer + frontend-engineer][Depends: 3.2–3.4][Gate: F5][Output: keyboard/focus/screen-reader、200% text、30–50% expansion、reduced motion、light/dark/narrow layout、ten-locale leaf translations与copy review evidence][Validation: focused Vitest/manual matrix通过；TOTP enrollment secret不进入snapshot/screenshot fixture]
- [ ] 3.6 [P0][Owner: frontend integration owner][Depends: 3.5][Gate: F6 serialized integration][Output: Settings → Account唯一fixed route/lazy mount、approved contextual CTA、locale index parity、style loader注册；feature state不进入AppShell root][Validation: shared route unions同时更新且有drift test；flag off无entry/empty slot/timer/network；`SettingsView`、locale indexes、style registries仅此owner修改]
- [ ] 3.7 [P0][Owner: product-design-owner + manual-qa-engineer][Depends: 3.6][Gate: G2][Output: multi-round user UX review、Mock scenario review记录与 `M0 — Account UI Mock Review Package`][Validation: zero real network/native calls；full Mock journeys/a11y/i18n/copy通过；verdict只能写 `UI accepted against Mock scenarios`，不得声称backend、Real integration或local trial完成]

## 4. Lane D — Doge Native Broker、vault/session 与 safe IPC

- [ ] 4.1 [P0][Owner: backend-runtime-engineer][Depends: 1.7, 2.2][Gate: D1][Output: Broker operation ledger、intent/request/operation identity binding、deadline/cancel/reconcile、closed error mapping与 wakeup-only event buffer/replay][Validation: duplicate/conflict、cancel-before-send、bytes-may-have-sent outcomeUnknown、early-before-ACK、duplicate/delayed/absent/old-epoch event tests]
- [ ] 4.2 [P0][Owner: data-storage-engineer + desktop-platform-engineer][Depends: 4.1][Gate: D2][Output: independent multi-account-ready Account SQLite v1、OS-vault-only stage/activate/delete generation saga、access-memory/refresh-generation、quarantine/startup reconcile；无session-only/fallback vault][Validation: new-secret→DB failure、DB-active→old-delete failure、locked/unavailable、orphan/missing ref、newer/corrupt/quick-check cases；authenticated projection仅在vault+metadata+`/auth/me` commit后出现]
- [ ] 4.3 [P0][Owner: desktop-platform-engineer][Depends: 4.1, 4.2][Gate: D3][Output: system-browser + loopback/app-link external continuation broker、PKCE/state/nonce/audience/device binding、single-instance owner、app running/background/not-running callback/reset intent handling][Validation: firewall/port collision、focus/cancel/timeout/replay/late return、early callback、wrong audience/device与no-secret URL/log matrix；未验证platform capability保持off]
- [ ] 4.4 [P0][Owner: backend-runtime-engineer][Depends: 4.1, 2.3 plan][Gate: D4][Output: fixed signed/build-channel HTTPS Token2Api Authority adapter、runtime envelope/schema validation、capability/guarantee negotiation、stable reason mapper与 stateful scenario transport][Validation: renderer/user/mock/env不能提供origin；unsupported major/missing guarantee/unknown mutation reason fail affected capability closed；no raw server message/body/header]
- [ ] 4.5 [P0][Owner: backend-runtime-engineer][Depends: 4.2–4.4, Lane T compatible test authority][Gate: D5][Output: complete auth/profile/security/session operations、refresh singleflight/accountEpoch、truthful local logout/remote unconfirmed、pull-only usage read与 Real-over-scenario Broker adapter][Validation: complete register/MFA/OAuth/reset/restore/logout/profile/TOTP/identity/revoke manifest projection；one refresh network rotation per wave；stale settlement commit=0]
- [ ] 4.6 [P0][Owner: dependency integration owner][Depends: 4.2, 4.3, approved dependencies][Gate: D6 serialized dependency merge][Output: reviewed OS vault/HTTP/SQLite/plugin dependencies与single `Cargo.lock` integration][Validation: dependency/license/platform review；transitive security crate不冒充vault evidence；其他lane不修改lockfile]
- [ ] 4.7 [P0][Owner: backend IPC integration owner][Depends: 4.5, 4.6][Gate: D7][Output: `state.rs → lib.rs → command_registry.rs` wiring、narrow Account commands/events、`src/services/tauri/account.ts`、shared barrel与exact invoke mapping tests][Validation: TS/Rust IPC round-trip；main-window/capability restriction；no raw secret/PII/path/diff；shared files仅此owner串行修改]

## 5. Lane T — token2api security spine、Desktop adapters 与 guarantees

- [ ] 5.1 [P0][Owner: token2api API owner][Depends: 2.3 approval ledger][Gate: B0/M1][Output: 从clean approved baseline同步fork `origin/main`并纳入upstream OAuth takeover fix `02e50cc22`，使用semantic merge保留fork customization][Validation: upstream regression + victim identity/profile unchanged、pending session unconsumed、no token issue；不得整文件ours/theirs]
- [ ] 5.2 [P0][Owner: token2api backend-runtime + data-storage owners][Depends: 5.1, 1.7][Gate: T1 session spine][Output: M2 durable-pair-only auth + atomic refresh/family tombstone/lost-response replay；M3 MFA atomic claim/log cleanup；M4 persisted revoke generation；M5 typed logout truth][Validation: 50+ concurrent refresh、crash points、reuse/restart/DB reload/Redis flush、concurrent MFA、logout Redis failure/already-revoked tests；access-only success不存在]
- [ ] 5.3 [P0][Owner: token2api API owner][Depends: 5.2][Gate: T2 Desktop completion][Output: generic A1 Desktop OAuth authorization/ticket/exchange、A2 password-reset handoff、按需T-GAP-12 human-verification handle；复用现有business services][Validation: MFA后才ticket；state/PKCE/audience/device/redirect/TTL/single-use/replay/App-vs-Web completion matrix；URL/log/DB无raw token]
- [ ] 5.4 [P0][Owner: token2api data-storage + security owners][Depends: 5.2][Gate: T3 selected API Key handoff][Output: M6 hash-for-auth + envelope-encrypted owner-recoverable credential、metadata-only list/get、account/device/recipe-bound Desktop handoff、deleted-audit purge phased migration，M7 omitted/clear/set ACL semantics][Validation: renderer/DB backup/idempotency/audit/log无plaintext；wrong owner/device/recipe/replay fail closed；legacy raw→hash+ciphertext migration、key rotation与rollback tests]
- [ ] 5.5 [P0][Owner: token2api API owner][Depends: 5.2–5.4][Gate: T4 Authority contract][Output: A3 version/guarantee descriptor、M8 stable reasons/429 envelope/Retry-After、M10 auth PII logging minimization；M9仅在future progress surface前完成][Validation: canonical negative scenarios以status+reason+retry hint映射；message drift不破坏client；exact deployed commit/version/guarantee response记录]
- [ ] 5.6 [P0][Owner: quality-engineer + security-privacy-reviewer][Depends: 5.1–5.5][Gate: Lane T readiness][Output: sanitized Authority fixtures、normalized traces、migration/rollback evidence与token2api release-blocker ledger][Validation: A0-required `T-GAP-01/02/03/04/05/08/09/11`与Codex `06/07`关闭；deployment若启用human verification则`12`关闭；`10/13`明确deferred而非伪完成]

## 6. Codex managed-key recipe、planner、transaction 与 runtime binding

- [ ] 6.1 [P0][Owner: Codex recipe/config owner][Depends: 1.3, 4.2, 5.4][Gate: C1][Output: immutable local `doge.account.codex-token-service/1` recipe、canonical target resolution、catalog digest、selected-key generation slot、manual-config preservation/collision policy与synthetic fixtures][Validation: server不能下发path/patch/template/command；未显式选择已有 API Key时不能plan；不读取/迁移现有plaintext `authJson`；unknown/malformed target fail closed]
- [ ] 6.2 [P0][Owner: Codex recipe/config owner][Depends: 6.1, 4.1][Gate: C2 read-only][Output: fingerprinted plan、TTL/accountEpoch/device/host/recipe/catalog/key-generation binding、changed-file safe labels与lazy semantic redacted detail；零写盘][Validation: no-config/manual-preserve/conflict/already-configured/noop/expired/concurrent-edit scenarios；raw path/content/diff不跨IPC]
- [ ] 6.3 [P0][Owner: Codex recipe/config owner + data-storage review][Depends: 6.2][Gate: C3 mutation][Output: exact-plan apply、same-filesystem stage/fsync/platform replace、journaled N-file rollback、durable receipt/recovery、separate FilesOutcome/ReloadOutcome][Validation: crash at every checkpoint、Nth-file failure、rollbackIncomplete、outcomeUnknown reconcile；terminal前必有durable receipt/recovery checkpoint]
- [ ] 6.4 [P0][Owner: Codex recipe/runtime owner][Depends: 6.3, 4.7][Gate: C4 runtime][Output: vault-to-child-process credential injection与provider-scoped new-session binding/reload；existing threads保持原binding][Validation: credential不进config/auth.json/argv/log/process diagnostics；new session使用正确profile/key；reload failure保留accurate file receipt与recovery]

## 7. G3 backend readiness 与 G4 cross-lane conformance

- [ ] 7.1 [P0][Owner: quality-engineer][Depends: 4.7, 5.6, 6.4][Gate: G3][Output: Lane D stateful fake/fault suite、Lane T server contract suite、Codex transaction suite与 Local Mode harness汇总][Validation: backend lanes各自green但status仅为`Lane D/T contract-ready`；flags off/signed-out/outage/vault fail/DB corrupt/session revoked/quota exhausted时Local Mode green]
- [ ] 7.2 [P0][Owner: test-automation-engineer][Depends: 3.7, 7.1][Gate: G4][Output: canonical scenarios通过 Mock Gateway、Real Gateway-over-scenario transport、Broker fake/real authority、token2api test authority的normalized trace conformance runner][Validation: Gateway-visible transitions/recovery/terminal semantics一致；unknown/negative cases fail closed；Mock/Real drift=0]
- [ ] 7.3 [P0][Owner: type-contract-reviewer + change-reviewer][Depends: 7.2][Gate: G4 review][Output: cross-language/schema/runtime mapping review与lane diff review][Validation: exact location/trigger/severity findings全部关闭；无private compatibility field、UI route branch或backend alternate manifest]

## 8. G5 Late Integration 与 Real E2E

- [ ] 8.1 [P0][Owner: integration owner][Depends: G2 (3.7), G4 (7.3)][Gate: G5][Output: composition-root-only `MockAccountGatewayV1 → RealAccountGatewayV1` swap、production account flags/fixed HTTPS origin/build-channel contract与internal cohort enablement][Validation: component/domain intent/DTO/journey无需改写；若需改动则退回G1/G2；Real failure绝不fallback Mock]
- [ ] 8.2 [P0][Owner: build-ci-engineer][Depends: 8.1][Gate: production boundary][Output: executable checks for production Real-only mode、Mock/Lab/scenario exclusion、fixed origin、flags-off zero startup/background calls、secret-free build metadata][Validation: production graph/chunks无法通过URL/env/localStorage选择Mock；bundle scan无fixture secret/PII/private URL]
- [ ] 8.3 [P0][Owner: quality-engineer + manual-qa-engineer][Depends: 8.1, 8.2][Gate: I0 Real integration][Output: real synthetic-account integration/E2E evidence for capability discovery、register/email code、login/MFA、enabled OAuth、forgot/reset、cold restore/refresh/logout、profile/security、pull-only usage、managed key与Codex plan/apply/restart][Validation: selected non-production authority exact commit/version/guarantees记录；unsupported provider hidden；至少一条complete access path；Local Mode完整]
- [ ] 8.4 [P0][Owner: security-privacy-reviewer][Depends: 8.3][Gate: I0 security][Output: end-to-end threat/source-to-sink review、strict account support-bundle与log/renderer/SQLite/vault/config/process artifact scan][Validation: forbidden secret/PII/raw path/diff canary=0；vault/session/callback/key/config/payment boundaries无blocking finding]
- [ ] 8.5 [P1][Owner: performance-reliability-reviewer][Depends: 8.3][Gate: I0 reliability][Output: Settings/Account interaction、closed/off/background render与request cadence evidence][Validation: no root timer/append store/polling regression；quota remains pull-only；account outage不增加Local Core startup dependency]

## 9. G6 acceptance、M0/A0 truth 与 macOS local package

- [ ] 9.1 [P0][Owner: release-engineer + build-ci-engineer][Depends: 8.4, 8.5][Gate: package source][Output: 从verified integrated commit建立独立clean package worktree/clone，记录commit、Node 20/npm、Rust/CMake、arm64 target、`OPENSSL_DIR` dylib arch与signature lane][Validation: clean `git status` before/after；`npm ci` + `npm ls --depth=0`无missing/invalid/extraneous；不清理shared dirty worktree]
- [ ] 9.2 [P0][Owner: quality-engineer][Depends: 9.1][Gate: package static][Output: full repository + account focused validation ledger][Validation: branding/docs/runtime/messages/engine/bundle/large-file gates、lint/typecheck/full tests、Rust tests、build/release contract tests、strict OpenSpec与`git diff --check`全绿]
- [ ] 9.3 [P0][Owner: release-engineer][Depends: 9.2][Gate: macOS ARM64 build][Output: production frontend、arm64 `doge.app`、daemon、bundled OpenSSL与DMG；优先validated locally-signed/non-notarized lane，若unsigned则标记internal-only limitation][Validation: app/daemon/dylib arm64、`@rpath`无host absolute path、Info.plist/resources、DMG verify/checksum；signed lane `codesign --verify --deep --strict`]
- [ ] 9.4 [P0][Owner: manual-qa-engineer][Depends: 9.3][Gate: G6 runtime][Output: target Mac install/first/cold/restart launch、flags-off/signed-out/outage/vault unavailable、Real auth/session/logout、pull-only usage、Codex apply/recovery、Mock exclusion与rollback smoke evidence][Validation: OS version/arch/commit/DMG SHA/signature state/authority environment记录但不记录credentials；Local Mode全部既有能力可用]
- [ ] 9.5 [P0][Owner: product-design-owner + doge-project-lead][Depends: 9.4][Gate: G6/A0][Output: Real UX与已批准M0 Mock UX等价性review、Closure Report 与 `A0 — Fully Integrated Local Trial Package` verdict][Validation: status明确为`local packaged trial complete`且只覆盖target macOS；不得声称all-platform GA、billing/device/multi-account/remote/additional recipes完成]

### M0 与 A0 不可混淆

- **M0 — Account UI Mock Review Package**：只证明deterministic Mock场景下的journey、visual、interaction、a11y与copy被接受，并且真实network/native calls为零。它不证明token2api readiness、vault/session security、callback、filesystem mutation、Real adapter、E2E或可试用集成完成。
- **A0 — Fully Integrated Local Trial Package**：证明Real adapter、selected token2api environment、target macOS package/install/launch、complete account access、pull-only usage、managed key与Codex recipe Real E2E，以及Local Mode invariant。只有9.5可使用`local packaged trial complete`。

## 10. Deferred Master Plan lanes（不阻塞 A0，但不得从计划删除）

- [ ] 10.1 [P1][Owner: product-spec-owner][Depends: A0][Gate: later change][Output: subscription progress correction/commerce management release change][Validation: `T-GAP-13` shape/partial truth关闭；billing/payment credential仍provider-hosted]
- [ ] 10.2 [P1][Owner: solution-architect][Depends: A0][Gate: later change][Output: device/session inventory、revoke-one、lost-device、account deletion/export与multi-account product/contract change][Validation: 不以local device label或local cleanup冒充remote truth；per-account vault/DB/receipt/dismiss isolation]
- [ ] 10.3 [P1][Owner: solution-architect + security-privacy-reviewer][Depends: A0][Gate: later change][Output: remote/daemon/web device-authorization、vault/unlock、BFF/host binding release change][Validation: no wrong-host mutation；browser不持refresh/API key；Desktop Local Mode独立]
- [ ] 10.4 [P1][Owner: one recipe owner per change][Depends: C0/C3 stable][Gate: Cn][Output: Claude/Gemini/OpenCode及其他recipe的独立versioned changes][Validation: 每个recipe独立fixtures、safe diff、apply/recovery/reload与platform evidence；不得remote下发arbitrary path/patch/command]

## 11. Governance closure（仅在实际实现完成后）

- [ ] 11.1 [P0][Owner: documentation-governance-owner][Depends: A0][Gate: governance][Output: Governance Closure Report，覆盖OpenSpec/Trellis/spec/docs/index、M0/A0、token2api deployed evidence、platform qualifiers、residual drift与deferred lanes][Validation: tasks/evidence与actual code/package一致；无false completion；适用strict docs/spec checks通过]
- [ ] 11.2 [P0][Owner: doge-project-lead][Depends: 11.1][Gate: user delivery][Output: final Closure Report与trial handoff][Validation: 用户目标、integrated outcome、validation evidence、remaining risk和next steps完整；未经授权不sync/archive/commit/push]
