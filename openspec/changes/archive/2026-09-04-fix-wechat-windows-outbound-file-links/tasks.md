# Tasks

- [x] 1. 修复 ordinary Markdown target 的 slash-prefixed Windows drive normalization。
- [x] 2. 支持 `.md` -> `text/markdown` outbound `file_item` classification。
- [x] 3. 增加 host-neutral native path、Windows screenshot-shape、Unix path-preservation 与 Markdown file focused regression tests。
- [x] 4. 更新 WeChat bundled bridge executable contract。
- [x] 5. 执行 Windows L3 focused verification，并显式记录 macOS/Linux native CI、真实设备与 L4 未覆盖范围。
- [x] 6. 修复 macOS `test-tauri` 中 workspace-unavailable regression 的 Windows-only absolute path fixture。
