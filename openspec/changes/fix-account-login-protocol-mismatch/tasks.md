# Tasks

- [x] 1. Capture production-safe evidence for authority negotiation and the failed login request.
- [x] 2. Add responsibility-layer regression tests for the exact mismatch.
- [x] 3. Implement the minimal contract fix without weakening fail-closed behavior.
- [x] 4. Verify focused contract/UI tests, typecheck/lint, runtime contracts, and production endpoint probes; token2api code is unaffected.
- [x] 5.1 Build the replacement macOS DMG and verify the exact image, signature structure, architecture, and bundled runtime libraries.
- [ ] 5.2 Launch-smoke the exact replacement after the currently running installed Doge instance is closed; do not run two instances against the same account state.
- [x] 6. Update executable contract/spec evidence and record the release session.
